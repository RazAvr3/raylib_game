specify compiler only in the first build.
if want to switch compiler, delete the cmake cache.


TODO:
compiler - delete cmake cache when switching compilers
           flags - try to add flags in seperate cmake file (flags.cmake)
clang-tidy
clang-format
CI/CD

start writing some code ....
unit tests

later stage:
package manager
web

