docker run -it --rm \
    -e DISPLAY=$DISPLAY \
    -v $PWD:/workspace \
    -v /tmp/.X11-unix:/tmp/.X11-unix \
    ubuntu-build-env:latest


docker build -t ubuntu-build-env .

cmake -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++ ..


cmake -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ ..



TODO:
git
build script with options
clang-tidy
clang-format
compiler - flags, optiomizations, debug version
CI/CD

start writing some code ....

later stage:
package manager
web

